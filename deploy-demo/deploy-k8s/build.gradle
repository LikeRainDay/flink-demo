jar {
    configurations.runtimeClasspath.collect { it.isDirectory() ? it : zipTree(it) }

    manifest {
        attributes 'Main-Class': 'com.finecloud.flink.k8s.EventCounterJob'

    }
}

dependencies {

}